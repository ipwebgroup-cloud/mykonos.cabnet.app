# Mykonos v2.6.11 — Response Packaging Panel

## Scope
- backend detail screen only
- changed-files-only patch
- no schema change
- no public theme change
- no list filter change

## What changed
- added a new **Response Packaging** panel on the **Internal** tab
- gives operators a read-only scan of what should go into the next guest-facing package
- separates **what to package** from **what to confirm** before the next outbound move
